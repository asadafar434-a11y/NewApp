feat(T-012): Better Auth — регистрация, вход, сессии

- src/lib/auth.ts: betterAuth с prismaAdapter, emailAndPassword, сессия 30 дней
  (updateAge — скользящее продление раз в сутки)
- sendVerificationEmail/sendResetPassword пока логируют URL (T-013/T-014 подключат письма)
- Смонтирован хэндлер app.on(['GET','POST'], '/api/v1/auth/*', ...)
- env.ts: BETTER_AUTH_SECRET (обязателен), BETTER_AUTH_URL
- package.json: dev-скрипт теперь грузит apps/api/.env через node --env-file — раньше
  наш собственный env.ts никогда не читал .env (маскировалось дефолтами всех полей),
  только Prisma CLI/Client грузили его сами для DATABASE_URL

Company создаётся хуком после регистрации из additionalFields.company. По пути обнаружены
и исправлены два реальных бага better-auth 1.6.25, не предположения:
- before-хук databaseHooks.user.create мержит result.data поверх исходных данных
  ({...actualData, ...result.data}), а не заменяет их — пропуск ключа в деструктуризации
  его не убирает, нужно явно вернуть { company: undefined }
- в before-хуке user.id ещё не сгенерирован (генерируется в адаптере непосредственно
  перед записью) — корреляция before/after сделана по email, а не по id

Проверено вживую на реальном Postgres (curl, JSON из файла — inline -d ломал кириллицу
в Git Bash на Windows):
- sign-up создаёт User + Company с точной кириллицей, без потери данных
- sign-in отдаёт Set-Cookie, GET /auth/get-session с этой кукой аутентифицирован (expiresAt +30д)
- неверный пароль и несуществующий email дают идентичный ответ 401 INVALID_EMAIL_OR_PASSWORD

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
