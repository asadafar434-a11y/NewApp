# T-036: Finance — записи и реальные графики

**Фаза:** 4 · **Оценка:** 4ч · **Зависит от:** T-016 · **Статус:** todo

## Цель
Владелец вносит доходы/расходы; KPI и оба графика Finance строятся из БД.

## Контекст
Схемы `finance.ts` — [05-api.md](../05-api.md); замены в UI — [06-frontend.md](../06-frontend.md)
(строка Finance.tsx). Сиды уже дают 6+ месяцев данных.

## Что сделать
- [ ] shared: `schemas/finance.ts` (уже описана) + campaign по образцу
- [ ] API: finance-records routes/service/repo (GET ?from&to&kind, POST, PATCH, DELETE hard); GET отдаёт также агрегаты по месяцам (`{month, incomeKopecks, expenseKopecks}[]`) — считает сервис одним groupBy
- [ ] Фронт: `api/finance.ts`; Finance.tsx — REVENUE_DATA/EXPENSE_DATA → агрегаты (выручка/прибыль по месяцам; расходы по категориям тек./пред. месяц); KPI (выручка, прибыль, маржа, изменение к пред. месяцу) — из тех же агрегатов
- [ ] Модалка «Добавить запись» (тип, категория-датчик с автодополнением существующих, сумма ₽, месяц, заметка) + таблица записей с удалением
- [ ] Vitest: агрегация по месяцам корректна (фикстуры), изоляция компаний

## Затрагиваемые файлы
- `apps/api/src/{routes,services,repositories}/finance-records.*` — создать
- `apps/web/src/api/finance.ts` — создать; `src/pages/Finance.tsx` — изменить

## Критерии приёмки
- [ ] После сидов графики выглядят содержательно; новая запись сразу двигает графики
- [ ] Удаление записи необратимо и пересчитывает графики
- [ ] Тесты зелёные

## Как проверить
UI: добавить доход 100 000 ₽ за текущий месяц → график и KPI изменились; F5.

## Подводные камни
Прибыль = доходы − расходы по месяцу (не хранится, только считается). Recharts ждёт рубли —
делить на 100 в селекторе данных, не в БД.
