# T-030: AI в Hiring — чат и черновики сообщений

**Фаза:** 3 · **Оценка:** 3ч · **Зависит от:** T-028, T-023 · **Статус:** todo

## Цель
Левая панель Hiring — реальный AI с контекстом заявки; MessageModal генерирует черновик сообщения специалисту.

## Контекст
Переиспользуются ai-tasks (kind=hiring) и стриминг из T-029. `AI_RESPONSES` и
`MESSAGE_TEMPLATES`-заглушки заменяются.

## Что сделать
- [ ] Системный контекст kind=hiring: активная заявка + список её специалистов со статусами
- [ ] Фронт: левая панель → тот же механизм, что NewTask (один hiring-диалог на заявку, создаётся лениво); quick-чипы отправляют реальные промпты
- [ ] MessageModal: кнопка «Сгенерировать с AI» → completeJson (`{subject?, text}`) по шаблону (приглашение/отказ/оффер) + профилю специалиста → textarea редактируемая → отправка в чат (T-023)
- [ ] Vitest: контекст hiring содержит специалистов заявки (мок ai)

## Затрагиваемые файлы
- `apps/api/src/services/ai-tasks.service.ts` — расширить контекст
- `apps/web/src/pages/Hiring.tsx` (панель AI, MessageModal) — изменить

## Критерии приёмки
- [ ] Вопрос «кого из кандидатов позвать на интервью?» — ответ ссылается на реальных специалистов
- [ ] Черновик приглашения упоминает имя/роль специалиста; после правки уходит в чат
- [ ] Лимит токенов действует и здесь

## Как проверить
UI: чат в левой панели + генерация черновика из CandidateDetail.

## Подводные камни
В AI-контекст не передавать email/телефон специалистов (07-auth-and-security, раздел 152-ФЗ).
Один hiring-диалог на заявку — ключ (requestId) хранить в AiTask.title или отдельном поле связи;
проще: title = requestId, поиск по нему.
