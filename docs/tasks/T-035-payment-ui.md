# T-035: UI оплаты и история платежей

**Фаза:** 4 · **Оценка:** 3ч · **Зависит от:** T-034 · **Статус:** todo

## Цель
Оплата консультации доступна из блока «Консультация»; страница /payments показывает историю.

## Контекст
Блок «Консультация» создан в T-027; связка с оплатой — здесь. Новый роут /payments —
[06-frontend.md](../06-frontend.md#карта-роутов).

## Что сделать
- [ ] `api/payments.ts`: usePayments, useCreatePayment
- [ ] Блок «Консультация»: бейдж оплаты (нет платежа → кнопка «Оплатить N ₽» → confirmationUrl в новой вкладке; pending → «Ожидает оплаты» + refresh; succeeded → зелёный «Оплачено»)
- [ ] SSE `payment:status` → инвалидация ['payments'] и бейджа
- [ ] Страница /payments: таблица (дата, заявка, специалист, сумма, комиссия, статус) в стилистике токенов; пункт в сайдбар Root
- [ ] «Отметить проведённой» (T-027) теперь требует succeeded-платежа (кнопка задизейблена с подсказкой)

## Затрагиваемые файлы
- `apps/web/src/api/payments.ts`, `src/pages/Payments.tsx` — создать
- `apps/web/src/pages/{Hiring,Root}.tsx`, `src/routes.tsx` — изменить

## Критерии приёмки
- [ ] Полный цикл в UI: «Оплатить» → тестовая карта → бейдж «Оплачено» (после refresh/вебхука)
- [ ] /payments показывает платёж; F5 ничего не теряет
- [ ] «Проведена» недоступна без оплаты

## Как проверить
UI-цикл с тестовой картой ЮKassa.

## Подводные камни
Возврат из ЮKassa (returnUrl) — на страницу заявки с параметром, показать «проверяем оплату»
и дёрнуть refresh. Суммы в UI — рубли с пробелами разрядов (существующий formatMoney-паттерн).
