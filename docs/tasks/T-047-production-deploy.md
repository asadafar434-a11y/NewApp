# T-047: Production — деплой по тегу

**Фаза:** 5 · **Оценка:** 3ч · **Зависит от:** T-046 · **Статус:** todo

## Цель
Тег `v*` разворачивает production; app.ДОМЕН работает на проде.

## Контекст
Отличия от staging: порт 3000, бакет orbital-prod, **пока тестовый** магазин ЮKassa (боевой —
в T-053 после юр. оформления), сиды НЕ прогоняются.

## Что сделать
- [ ] `/opt/orbital/prod/`: compose + .env с прод-значениями; отдельный POSTGRES_PASSWORD
- [ ] Проверить ветку prod в deploy.sh и deploy.yml (условие на tag)
- [ ] Caddyfile: app-блок (уже из T-045) → reverse_proxy 127.0.0.1:3000
- [ ] `git tag v0.1.0 && git push --tags` → прогнать релиз
- [ ] Чек прод-меню: регистрация (реальное письмо), создание заявки, чат, звонок
- [ ] Зафиксировать процедуру отката в этом файле: `./deploy.sh v0.0.X` с предыдущим тегом

## Затрагиваемые файлы
- VPS: /opt/orbital/prod/**

## Критерии приёмки
- [ ] app.ДОМЕН полностью работает; staging и prod независимы (разные БД — проверить, что регистрация на одном не видна на другом)
- [ ] Повторный деплой того же тега идемпотентен
- [ ] Откат на предыдущий тег работает (проверить один раз)

## Как проверить
Тег → Actions → прод-смоук руками → откат → возврат.

## Подводные камни
`migrate deploy` на проде необратим — правило двух фаз из [09-conventions.md](../09-conventions.md#миграции)
действует с этого момента строго. Секреты прод-.env не совпадают со staging (BETTER_AUTH_SECRET!).
