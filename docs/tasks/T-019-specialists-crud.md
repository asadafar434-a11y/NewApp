# T-019: Специалисты — CRUD + форма

**Фаза:** 3 · **Оценка:** 4ч · **Зависит от:** T-018 · **Статус:** todo

## Цель
Владелец добавляет специалиста к заявке через форму; профиль сохраняется в БД.

## Контекст
Схемы `specialist.ts` — [05-api.md](../05-api.md#zod-схемы-ядра). Поля повторяют существующий
тип `Candidate` из Hiring.tsx.

## Что сделать
- [ ] shared: `schemas/specialist.ts`
- [ ] API: repo/service/routes по образцу T-018 (list c фильтрами `requestId/status/search` + курсор; create проверяет, что requestId принадлежит компании; get отдаёт + statusChanges + calls)
- [ ] Сервис create: сразу создаёт пустой Conversation для специалиста
- [ ] Vitest: создание, чужой requestId → 404, поиск по имени/роли, пагинация
- [ ] Фронт: `api/specialists.ts`; модалка «Добавить специалиста» (имя, роль, email обязательные; остальное — свернуто в «дополнительно»); кнопка в средней панели Hiring

## Затрагиваемые файлы
- `packages/shared/src/schemas/specialist.ts` — создать
- `apps/api/src/{routes,services,repositories}/specialists.*` — создать
- `apps/web/src/api/specialists.ts`, компонент формы, `Hiring.tsx` — создать/изменить

## Критерии приёмки
- [ ] Специалист из формы появляется в БД вместе с Conversation
- [ ] Валидация: без email форма не уходит; сервер дублирует проверку (400)
- [ ] Тесты зелёные

## Как проверить
UI + Studio (Conversation создан) + тесты.

## Подводные камни
`skills` — массив строк: в форме поле «через запятую» с трансформацией. Email специалиста
не уникален глобально (один человек может быть у разных компаний) — уникальность не ставить.
