# T-041: Dashboard — реальные KPI и задачи

**Фаза:** 4 · **Оценка:** 3ч · **Зависит от:** T-029, T-020 · **Статус:** todo

## Цель
Дашборд показывает реальную сводку компании вместо моков.

## Контекст
`GET /dashboard` — [05-api.md](../05-api.md); карта замен — [06-frontend.md](../06-frontend.md)
(строка Dashboard.tsx). Уведомления — отдельно в T-042.

## Что сделать
- [ ] `dashboard.service.ts`: один метод — параллельные count/aggregate: AI-задач за месяц, активных заявок, специалистов в работе (не new/rejected/финальные), потрачено токенов из /ai/usage; последние 5 AiTask (+kind, дата); дельты к прошлому месяцу
- [ ] Роут GET /dashboard (схема в shared)
- [ ] Фронт: STATS → KPI из ответа (подписи адаптировать: «Задач AI за месяц», «Активные заявки», «Специалистов в работе», «Токены: N%»); TASKS → последние AiTask с навигацией по kind; dismissed — локальный state как сейчас
- [ ] Пустое состояние нового аккаунта: карточки с нулями + CTA «Создайте первую заявку»
- [ ] Vitest: агрегаты на фикстурах

## Затрагиваемые файлы
- `apps/api/src/{services,routes}/dashboard.*` — создать
- `apps/web/src/api/dashboard.ts` — создать; `src/pages/Dashboard.tsx` — изменить

## Критерии приёмки
- [ ] Числа соответствуют данным (проверить руками против Studio)
- [ ] Новый пользователь видит нули и CTA, а не ошибку
- [ ] Клик по недавней задаче ведёт в правильный раздел

## Как проверить
Сиды → сверить цифры; новый аккаунт → пустое состояние.

## Подводные камни
Один HTTP-запрос на всё (не 6 отдельных) — иначе дашборд медленный. staleTime 30 с достаточно.
