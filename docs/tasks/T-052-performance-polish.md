# T-052: Производительность и полировка UX

**Фаза:** 6 · **Оценка:** 4ч · **Зависит от:** T-050 · **Статус:** todo

## Цель
p95 < 300 мс на основных списках; скелетоны/пустые состояния/ошибки везде единообразны.

## Что сделать
- [ ] Наполнить staging объёмом NFR-1 (скрипт: 100 специалистов, 2000 сообщений, 500 финзаписей)
- [ ] `EXPLAIN ANALYZE` тяжёлых запросов (списки, диалоги с unread, дашборд-агрегаты) — убедиться, что индексы из 04 используются; добавить недостающие миграцией
- [ ] Пройти все экраны: скелетон при загрузке, «повторить» при ошибке, пустое состояние, консистентные тосты — добить пропуски
- [ ] N+1-проверка: Prisma-логи query в dev на каждом экране; включить select/include точечно
- [ ] Lighthouse на прод-сборке: bundle-размер, code-splitting страниц через lazy() при необходимости

## Затрагиваемые файлы
- `apps/api/prisma/migrations/**` — возможные индексы
- точечные правки страниц web

## Критерии приёмки
- [ ] На наполненном staging списки и дашборд открываются < 300 мс (Network, p95 на глаз по 10 обновлений)
- [ ] Ни одного экрана без обработки loading/error/empty (чек-лист по 8 экранам в этом файле)
- [ ] Нет запросов-простыней в Prisma-логе (N+1)

## Как проверить
Staging + DevTools Network/Throttling; чек-лист экранов.

## Подводные камни
Индекс не используется при несовпадении порядка колонок с фильтром — сверять план, не веру.
Не оптимизировать то, что и так быстрое: фиксировать замеры до/после в этом файле.
