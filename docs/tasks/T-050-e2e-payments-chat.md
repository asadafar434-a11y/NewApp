# T-050: e2e — оплата консультации и magic-чат

**Фаза:** 6 · **Оценка:** 4ч · **Зависит от:** T-047, T-035 · **Статус:** todo

## Цель
Playwright покрывает два оставшихся критичных сценария: платёж и полный чат-цикл специалиста.

## Контекст
Инфраструктура e2e — из T-032. Платёж в e2e: ЮKassa-редирект мокается (env PAYMENTS_MOCK=1 →
lib/payments возвращает фейковый confirmationUrl, refresh сразу отдаёт succeeded) — реальную
тестовую карту в CI не гоняем; ручной прогон с картой — на staging.

## Что сделать
- [ ] PAYMENTS_MOCK-режим в lib/payments (по образцу AI_MOCK)
- [ ] Тест «оплата»: consult-заявка → специалист → «Оплатить» → (мок) → бейдж «Оплачено» → звонок → «проведена» + итог → /payments показывает succeeded
- [ ] Тест «magic-чат»: сообщение → ссылка → новый context → ответ → статусы read у владельца → уведомление появилось
- [ ] Ручной чек-лист staging (в этом файле): реальная тестовая карта, реальный вебхук

## Затрагиваемые файлы
- `e2e/payments.spec.ts`, `e2e/magic-chat.spec.ts` — создать
- `apps/api/src/lib/payments/index.ts` — mock-режим

## Критерии приёмки
- [ ] Оба теста зелёные локально и в CI
- [ ] Ручной прогон по чек-листу на staging пройден один раз (галочки в этом файле)

## Как проверить
```bash
pnpm --filter e2e test
```

## Подводные камни
Мок платежей не должен просачиваться в прод: PAYMENTS_MOCK читается только при NODE_ENV!=production
(захардкодить условие, покрыть тестом).
