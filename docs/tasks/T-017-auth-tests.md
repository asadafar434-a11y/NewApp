# T-017: Тесты auth-флоу

**Фаза:** 2 · **Оценка:** 3ч · **Зависит от:** T-016 · **Статус:** todo

## Цель
Интеграционные тесты закрывают регистрацию, вход, verify-гейт и изоляцию компаний.

## Контекст
Политика тестов — [09-conventions.md](../09-conventions.md#тесты). Это первые интеграционные
тесты с реальной БД — заложить инфраструктуру: тестовая БД `orbital_test`, очистка между тестами.

## Что сделать
- [ ] `test/helpers/db.ts`: подключение к orbital_test, truncate всех таблиц в beforeEach
- [ ] `test/helpers/auth.ts`: `registerAndLogin()` — возвращает куку и ids (переиспользуется всеми будущими тестами)
- [ ] Тесты: регистрация создаёт Company; вход возвращает куку; /me без куки 401; /me до verify 403; после verify 200
- [ ] Тест-заготовка изоляции: пользователь A не видит данных B (пока на /me; расширится в T-018+)

## Затрагиваемые файлы
- `apps/api/test/helpers/**`, `test/auth.test.ts` — создать
- `apps/api/vitest.config.ts` — env тестовой БД

## Критерии приёмки
- [ ] `pnpm --filter @orbital/api test` зелёный локально и в CI
- [ ] Тесты независимы: любой порядок и повторный запуск проходят

## Как проверить
```bash
pnpm --filter @orbital/api test
```

## Подводные камни
Truncate быстрее пересоздания схемы; порядок таблиц не важен с `TRUNCATE ... CASCADE`.
В CI DATABASE_URL указывает на сервис-контейнер — уже настроено в T-006/T-007.
