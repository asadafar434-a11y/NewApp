# T-020: Hiring на реальных данных

**Фаза:** 3 · **Оценка:** 4ч · **Зависит от:** T-019 · **Статус:** todo

## Цель
Экран Hiring целиком работает от БД: список, поиск, фильтры, детальная панель.

## Контекст
Карта изменений — [06-frontend.md](../06-frontend.md#что-менять-в-существующем-ui), строка
Hiring.tsx. Мок `ALL_CANDIDATES` удаляется; демо-данные приходят из сидов (T-011).

## Что сделать
- [ ] `Hiring.tsx`: `ALL_CANDIDATES` → `useSpecialists({ requestId, status, search })`; серверные фильтры вместо клиентских
- [ ] Детальная панель → `useSpecialist(id)`; скелетон при загрузке
- [ ] Правка профиля (существующие поля в CandidateDetail) → useUpdateSpecialist
- [ ] Удаление специалиста (кнопка в деталях) → soft-delete + инвалидация
- [ ] Пустые состояния: нет заявки → «создайте заявку»; нет специалистов → «добавьте первого»
- [ ] Ошибки запросов → инлайн «повторить»; мутаций → toast (sonner)

## Затрагиваемые файлы
- `apps/web/src/pages/Hiring.tsx` — существенно изменить
- `apps/web/src/api/specialists.ts` — дополнить

## Критерии приёмки
- [ ] После `prisma db seed` экран выглядит как раньше с моками
- [ ] Поиск и фильтр статуса дёргают сервер (видно в Network) и работают
- [ ] Изменение профиля сохраняется после F5

## Как проверить
Сиды → UI: поиск «Анна», фильтр hired, правка зарплаты, F5.

## Подводные камни
Дебаунс поиска (300 мс), иначе запрос на каждый символ. Не терять существующие стили/анимации —
менять только источник данных, не разметку.
