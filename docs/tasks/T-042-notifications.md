# T-042: Уведомления

**Фаза:** 4 · **Оценка:** 4ч · **Зависит от:** T-024, T-034 · **Статус:** todo

## Цель
Реальные уведомления: новое сообщение специалиста, звонок скоро, платёж прошёл; бейджи в сайдбаре живые.

## Контекст
Модель Notification — [04-data-model.md](../04-data-model.md); эндпоинты — [05-api.md](../05-api.md).
Дропдаун на Dashboard и Bell в Root уже нарисованы.

## Что сделать
- [ ] `notifications.service.ts`: `notify(companyId, type, text, link)` — создание + SSE `notification:new`; вызовы из: messages.service (сообщение специалиста), payments.service (succeeded), calls (за 1 час до звонка — node-cron проверка раз в 10 мин, флаг чтобы не дублировать)
- [ ] Роуты: GET /notifications?unread, POST /:id/read, POST /read-all
- [ ] Фронт: `api/notifications.ts`; дропдаун Dashboard → реальные данные + мутации read; Bell-счётчик Root и бейдж Messages (unread диалоги) — из queries; SSE → инвалидация
- [ ] node-cron в API (первый фоновый процесс): каждые 10 мин — звонки в ближайший час без отправленного уведомления
- [ ] Vitest: notify создаёт запись; read-all обнуляет; cron-функция идемпотентна

## Затрагиваемые файлы
- `apps/api/src/{services,routes}/notifications.*`, `src/lib/cron.ts` — создать
- `apps/web/src/api/notifications.ts` — создать; `src/pages/{Dashboard,Root}.tsx` — изменить

## Критерии приёмки
- [ ] Ответ специалиста с magic-страницы → уведомление у владельца без перезагрузки
- [ ] Успешная оплата → уведомление; клик ведёт на /payments
- [ ] «Прочитать все» гасит бейджи

## Как проверить
Два окна (владелец + инкогнито-специалист) → сообщение → колокольчик. Тестовая оплата → уведомление.

## Подводные камни
Дубликаты от cron: хранить признак «уведомление о звонке X отправлено» (link содержит callId —
проверка существования по link). SSE-событие не заменяет запись в БД — сначала БД, потом publish.
